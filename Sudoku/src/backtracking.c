#include "backtracking.h"

static int clamp(int val, int max){

}

void SolveGame(int *board, int sudokuDim){
    int targetMax = sudokuDim;
    int index = 0;

    int *startValues = malloc(sizeof(int) * sudokuDim * sudokuDim);
}